A(a : []int, b : int | y : int) {
    y := -1 ;
    var i : int {
        i := 0 ;
        while i < #a && y = -1 do {
            if a[i] = b then {
                y := i
            } else {
                i := i + 1
            }
        }
    } ;
    assert y = -1 || a[y] = b
}